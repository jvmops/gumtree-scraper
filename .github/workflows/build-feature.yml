name: build-feature

on:
  push:
    branches:
      - 'feature/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: git checkout
      uses: actions/checkout@v1
    - name: JDK 14 setup
      uses: actions/setup-java@v1
      with:
        java-version: 14-ea
